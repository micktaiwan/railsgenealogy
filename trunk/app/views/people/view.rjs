id = @person.id.to_s

# insert graphically the element
page << "if(!$('person_"+id+"')) {"
page.insert_html :bottom, 'graph', {:partial => 'person', :object => @person}
page << "}"

# then insert it in the graph object
page << "p = graph.add_person("+id+");"

page << "p.highlight();"
#page << "p.add_family();"

